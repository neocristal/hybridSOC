name: API Discovery Scan

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
    inputs:
      api-endpoint:
        description: "API discovery endpoint URL (overrides secret if provided)"
        required: false
        type: string
      api-token:
        description: "API discovery token (overrides secret if provided)"
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  api-discovery:
    name: Run API discovery action
    runs-on: ubuntu-latest
    env:
      # Configure these in repository settings -> Secrets and variables -> Actions
      # - API_DISCOVERY_ENDPOINT
      # - API_DISCOVERY_TOKEN
      API_ENDPOINT: ${{ inputs['api-endpoint'] || secrets.API_DISCOVERY_ENDPOINT }}
      API_TOKEN: ${{ inputs['api-token'] || secrets.API_DISCOVERY_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate API discovery configuration
        run: |
          if [ -z "$API_ENDPOINT" ]; then
            echo "Missing API endpoint. Set workflow input 'api-endpoint' or secret API_DISCOVERY_ENDPOINT."
            exit 1
          fi
          if [ -z "$API_TOKEN" ]; then
            echo "Missing API token. Set workflow input 'api-token' or secret API_DISCOVERY_TOKEN."
            exit 1
          fi

      - name: API discovery action
        uses: apisec-inc/apisec-run-scan@025432089674a28ba8fb55f8ab06c10215e772ea
        with:
          apisec-username: ${{ secrets.apisec_username }}
          apisec-password: ${{ secrets.apisec_password }}
          apisec-project: "VAmPI"
          sarif-result-file: "apisec-results.sarif"
          api-endpoint: ${{ env.API_ENDPOINT }}
          api-token: ${{ env.API_TOKEN }}

      - name: Upload SARIF report
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ./apisec-results.sarif

      - name: Add workflow summary
        run: |
          {
            echo "## API Discovery Scan";
            echo "- Endpoint: \`$API_ENDPOINT\`";
            echo "- Trigger: \`${{ github.event_name }}\`";
            echo "- Result: SARIF uploaded";
          } >> "$GITHUB_STEP_SUMMARY"
